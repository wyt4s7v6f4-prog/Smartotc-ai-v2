# Smartotc-ai-v2

## Auto signal mode
Select only the market. The engine automatically chooses the indicator feed and trade duration, then issues BUY/SELL immediately. OTC/FX uses 1-minute indicators with 30-second expiry; crypto uses 1-minute indicators with 60-second expiry. The UI no longer requires a timeframe selector.
